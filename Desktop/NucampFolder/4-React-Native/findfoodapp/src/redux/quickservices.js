import { QUICKSERVICES } from '../shared/quickservices';

export const quickServices = (state = QUICKSERVICES, action) => {
    switch (action.type) {
        default:
          return state;
      }
};